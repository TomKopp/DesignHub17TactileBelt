namespace DesignHub
{
static const int TICKS_PER_SECOND = 20;
static const int DEBOUNCEDELAY = 1000 / TICKS_PER_SECOND; // In milliseconds
static const int MAX_SKIPPEDFRAMES = 10;

static const int FORCE_CHAR_BUFFER_SIZE = 3; // In byte
}
